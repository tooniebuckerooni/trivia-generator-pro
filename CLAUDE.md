# Working preferences for this repo

- Whenever you reference a link the user would otherwise have to dig for (a deploy URL, a dashboard link, a worker/API endpoint, a checkout URL, etc.), always state the actual link in your reply. Don't just say "it's in the code" or "check the dashboard" — surface it directly so the user never has to go search for it themselves.
